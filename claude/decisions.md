# Лог решений — [Название проекта]

> Этот файл — «эстафетная палочка» между участниками.
> После каждой рабочей сессии добавляй запись внизу и синхронизируй файл.
> Claude загружает его в чат, чтобы понимать, что уже сделано и почему.

---

## Как добавлять запись

Скопируй шаблон блока ниже, заполни и добавь в конец файла.
Не удаляй старые записи — история важна.

```
### [YYYY-MM-DD] [Имя / Ник]

**Что сделал:**
- [пункт]

**Ключевые решения:**
- [решение] — [почему так, а не иначе]

**Что не получилось / отложил:**
- [пункт]

**Что нужно сделать следующему:**
- [ ] [задача]

**Важные фрагменты кода (если есть):**
[вставь сюда, если Claude должен знать конкретную реализацию]
```

---

## Записи

### [YYYY-MM-DD] [Имя]

**Что сделал:**
- Инициализировал репозиторий, настроил структуру папок
- Добавил docker-compose для локального запуска

**Ключевые решения:**
- Использовали FastAPI вместо Django — меньше бойлерплейта для API
- PostgreSQL в Docker, не SQLite — сразу близко к продакшену

**Что не получилось / отложил:**
- Не успел настроить Alembic, оставил на следующую сессию

**Что нужно сделать следующему:**
- [ ] Настроить Alembic и создать первую миграцию для таблицы users
- [ ] Проверить, что `docker-compose up` поднимается без ошибок

**Важные фрагменты кода:**
```python
# backend/main.py — точка входа, пока минимальная
from fastapi import FastAPI
app = FastAPI()

@app.get("/health")
def health():
    return {"status": "ok"}
```

---

<!-- Добавляй новые записи ниже этой строки -->
